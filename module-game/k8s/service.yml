apiVersion: v1
kind: Service
metadata:
  name: game
  namespace: ticketable-project
spec:
  selector:
    app: game
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP