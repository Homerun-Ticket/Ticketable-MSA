apiVersion: v1
kind: Service
metadata:
  name: ticket
  namespace: ticketable-project
spec:
  selector:
    app: ticket
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP